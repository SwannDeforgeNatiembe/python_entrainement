# Outil-Cool (Python)

Programme autonome sans dépendances : navigation de fichiers, serveur HTTP, scan TCP, notes chiffrées et utilitaires.  
Pour le lancer : `python3 multi_tool.py`  
Les notes sont stockées dans `~/.multi_tool_notes.db`.  
Quitter : option `0` dans le menu.
